
export enum tagType {
    notification = "notification",
    transaction = "transaction",
    auth = "auth",
    
  }
  export const tagTypeList = [
    tagType.notification,
    tagType.transaction,
    tagType.auth
   
  ];